import { ForLoggedInUserDirective } from './for-logged-in-user.directive';

describe('ForLoggedInUserDirective', () => {
  it('should create an instance', () => {
    const directive = new ForLoggedInUserDirective();
    expect(directive).toBeTruthy();
  });
});
